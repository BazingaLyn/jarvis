definitions:
  model.Response:
    properties:
      code:
        type: integer
      data:
        type: object
      msg:
        type: string
    type: object
info:
  contact: {}
  license: {}
paths:
  /movie/get/{id}:
    get:
      consumes:
      - application/x-json-stream
      parameters:
      - description: 电影id
        in: path
        name: id
        required: true
        type: integer
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/model.Response'
      summary: 获取某个电影的信息
      tags:
      - 电影模块
swagger: "2.0"
